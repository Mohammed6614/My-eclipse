# Example environment file for server
# Copy to .env and fill values for production
PORT=3000
# Optional: override front-end verify page base URL
FRONTEND_VERIFY_BASE=http://localhost:5500/My_moon/verify.html

# SMTP for production (optional). If not set, Ethereal test account will be used.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# Clinic admin email to receive booking notifications
ADMIN_EMAIL=admin@drbensefia.nl
# Copy to .env and fill your SMTP credentials for production testing
# If you leave these empty the server will create an Ethereal test account automatically
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=

# Public URL for the frontend verify link - change if you host differently
FRONTEND_VERIFY_BASE=http://localhost:5500/My_moon/verify.html

# Server port
PORT=3000
